let population = 256
let n= 2

function futurePeople (population,n) {
    let newPeople =n *360
    let findPop = population + newPeople
    return findPop
}

console.log(futurePeople(population,n))

// same as 


function futurePeople (population,n) {
    let newPeople =n *360
    let findPop = population + newPeople
    return findPop
}

console.log(futurePeople(3248,6))
